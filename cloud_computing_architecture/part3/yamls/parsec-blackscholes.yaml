apiVersion: batch/v1
kind: Job
metadata:
  name: parsec-blackscholes
  labels:
    name: parsec-blackscholes
spec:
  template:
    spec:
      containers:
      - image: anakli/cca:parsec_blackscholes
        name: parsec-blackscholes
        imagePullPolicy: Always
        command: ["/bin/sh"]
        args: ["-c", "taskset -c ${JOB_CORES} ./run -a run -S parsec -p blackscholes -i native -n ${JOB_CORES_N}"]
      restartPolicy: Never
      nodeSelector:
        cca-project-nodetype: "${JOB_NODE}"
